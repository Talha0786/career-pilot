# 021 — Drizzle schema + migration 0002: profile & document tables

**Status:** TODO | **Milestone:** M3

## Objective
Executable schema for `career_profiles`, `profile_sections`, `documents`, `document_versions` per design §2. Additive migration only — does not touch M2 tables.

## Files affected
- `packages/infrastructure/src/db/schema/index.ts` (extend with new `pgTable` exports)
- `packages/infrastructure/src/db/migrations/0002_profile_documents.sql`
- `packages/infrastructure/src/db/repositories/{profile,document}.repository.ts` implementing the ports from task 020

## Dependencies
020, 006

## Acceptance criteria
- [ ] `career_profiles.embedding vector(768)` (match the M2 schema's actual dimension note, not the design doc's stale 1024 figure — same correction already applied to `job_postings`)
- [ ] `profile_sections`: FK to profile, `kind` enum, `sort int`, `content jsonb`, generated `content_text`/tsvector for FTS per design §2 rationale
- [ ] `documents.deleted_at` nullable (soft delete, per design rule: "soft delete only where product requires undo (documents)")
- [ ] `document_versions` has no `updated_at` column and no UPDATE path in the repository — append-only enforced at both domain (019) and repository layer
- [ ] Migration runs clean on top of 0001 from an empty DB; `drizzle-kit check` passes

## Test plan
Integration (real Postgres via service container/Testcontainers, matching task 007's pattern): migrate from scratch, assert tables/indexes exist; repository round-trip tests (create profile → add section → read back; create document → add two versions → assert version 1 immutable).
