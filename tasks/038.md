# 038 — Match rubric scoring pipeline

**Status:** DONE | **Milestone:** M5

## Objective
Implement the match-scoring pipeline from `docs/06-agent-design.md` §3 ("Match rubric scoring | pipeline | mid | ScoreComponents schema | embedding prefilter caps volume"): embedding-based ANN prefilter (task 036) narrows candidates, then a guarded LLM call scores each candidate against a rubric, persisted and surfaced on the board.

## Files affected
- `packages/contracts/src/matching.ts` (new) — `ScoreComponentsSchema` (e.g. `{ skillsMatch: 0-1, experienceMatch: 0-1, seniorityFit: 0-1, overall: 0-1, rationale: string }` — check `docs/01-system-design.md`/`docs/02-database-design.md` for any prior rubric-dimension spec before inventing dimensions from scratch), `MatchScoreDtoSchema`.
- `packages/domain/src/matching/match-score.ts` (new) — `MatchScore` value object/aggregate: `{ id, profileId, jobPostingId, components: ScoreComponents, computedAt, factsHash /* profile factsHash at scoring time, for staleness */, embeddingModel }`.
- `packages/application/src/ports/repositories.ts` — add `MatchScoreRepository` port.
- `packages/infrastructure/src/db/migrations/0005_match_scores.sql` (new) — `match_scores` table, unique on `(profile_id, job_posting_id)`, upsert-on-recompute.
- `packages/infrastructure/src/db/repositories/drizzle-match-score.repository.ts` (new).
- `packages/application/src/matching/commands/score-match.ts` (new) — orchestrates: ANN prefilter (036) → for each candidate, load `prompts/match-score/v1.md` (034) → guarded `llm.complete()` → validate against `ScoreComponentsSchema` (zod; one repair-attempt on validation failure per ADR-006) → persist via `MatchScoreRepository`.
- `apps/worker/src/main.ts` — wire a handler (job-triggered, e.g. on profile embed completion or on-demand) to run `score-match` for a profile against its ANN-prefiltered candidate set.
- `apps/api/src/app.ts` — first place `AppDeps` needs a `GuardedLlmPort` + `MatchScoreRepository`; add `apps/api/src/routes/matching.ts` with an on-demand `POST /profiles/:id/rescan` or similar (check `docs/01-system-design.md` for any existing route-naming convention for this feature before inventing one).
- `packages/contracts/src/board.ts` — extend `ApplicationCardSchema` (or the job-posting-facing DTO) with an optional `matchScore` field so the board can surface it.
- Tests: unit (packages/application, fake LLM + fake repos), integration (real Postgres — persist/query/upsert), API integration test for the new route.

## Dependencies
- 033 (real CostEstimator — this is the first production `complete()` call site, budget accuracy matters).
- 034 (PromptStore).
- 035 (profile embeddings must exist to prefilter against).
- 036 (ANN prefilter).

## Acceptance criteria
- Given a profile and a set of embedded job postings, `score-match` returns rubric scores for the ANN-prefiltered top-N only (not every job posting — this is the whole point of the prefilter, keep it enforced with a test, not just a comment).
- Invalid/malformed LLM JSON output triggers exactly one repair retry, then a typed failure (not a crash) — matches the ADR-006 "structured outputs validated with zod; on validation failure → one repair attempt → fail the job with raw output stored for triage" rule verbatim.
- Scores are queryable per profile and surface on the board DTO.
- Budget guard is exercised on every call (no bypass path).

## Test plan
- Unit: prefilter-then-score orchestration with fakes; zod-repair-then-fail path with a deliberately malformed fake LLM response.
- Integration: real Postgres persistence + upsert-on-recompute.
- API integration: rescan route round-trip.

## Status
DONE.

**Rubric dimensions (judgment call):** `docs/02-database-design.md`'s `match_scores.components` lists
`{skills, seniority, domain, location, compensation}`. Swapped `compensation` for `experience`
(salary data is frequently absent from postings — an unreliable dimension to score against — while
years/type of experience is almost always inferable) and kept skills/seniority/domain/location,
matching what was ALREADY committed in task 034's real `prompts/match-score/v1.md` content
(`{skills, experience, seniority, domain, location, overall, rationale}`). Documented in
`packages/domain/src/matching/match-score.ts`'s doc comment, not silently diverged.

**Persistence shape (judgment call):** the design doc's `match_scores` sketch is unique on
`(job_posting_id, profile_id, method)`, append-many, "latest wins by created_at". Task 038's own
file list says unique on `(profile_id, job_posting_id)`, upsert-on-recompute — followed the task's
explicit instruction (simpler; this milestone only ever produces one scoring method) over the
older doc, documented in `MatchScoreRepository`'s doc comment and the migration file.

**Route design (judgment call):** the task file says `AppDeps needs a GuardedLlmPort +
MatchScoreRepository`. Wired `MatchScoreRepository` into `apps/api`'s `AppDeps` (real use: `GET
/profile/matches`) but deliberately did NOT add `GuardedLlmPort` there — every other LLM pipeline
in this codebase (job/profile embedding) keeps the LLM boundary exclusively in the worker process;
forking that boundary into the API for one route, which would also block an HTTP request on 10-20+
sequential mid-tier completions, contradicts that established pattern for no real benefit.
`POST /profile/rescan` enqueues `matching.score_requested`; `apps/worker`'s new
`createScoreMatchWorker` does the actual scoring. Routes are `/profile/rescan` and
`/profile/matches` (unprefixed, no id in the URL), following task 022's already-established
precedent over the task file's literal `/profiles/:id/rescan` suggestion — `docs/01-system-design.md`
has no route convention for this feature to check against.

**Trigger design:** implemented BOTH triggers the task file mentions as alternatives — "job-triggered"
(the profile-embed worker handler now calls an `onEmbedded` hook that auto-enqueues a rescan once a
profile's embedding is ready — `apps/worker/src/handlers/profile-facts-changed.handler.ts`) AND
"on-demand" (the API route above).

**Files:** `packages/contracts/src/matching.ts` (`ScoreComponentsSchema`, `MatchScoreDtoSchema`);
`packages/domain/src/matching/match-score.ts` (`MatchScore` value object, not an AggregateRoot — no
events, immutable snapshot, recompute = new instance); `packages/application/src/ports/repositories.ts`
(`MatchScoreRepository`); `packages/infrastructure/src/db/migrations/0005_match_scores.sql` +
`DrizzleMatchScoreRepository`; `packages/application/src/matching/commands/score-match.ts`
(ANN-prefilter-then-score orchestration, own zod validator — application already depends on zod
directly, deliberately not importing the contracts-package schema across the app/interface
boundary) + `request-match-scan.ts` (enqueue) + `queries/list-matches.ts` (read); worker handlers
`score-match.handler.ts` + extended `profile-facts-changed.handler.ts`; `apps/api/src/routes/matching.ts`;
`packages/application/src/pipeline/queries/get-board.ts` extended with optional `matchScore` (additive,
board still works unchanged without the new deps). `apps/worker/Dockerfile` now `COPY prompts ./prompts`
(verified present at `/app/prompts` in the built image — worker's `PROMPTS_DIR` resolves via
`import.meta.url`, not `process.cwd()`, so it's correct under both `tsx src/main.ts` locally and the
Docker image's `WORKDIR /app/apps/worker`).

**Evidence:**
- Unit (`packages/application/test/unit/score-match.test.ts`, fakes only): 7/7 — full prefilter-then-score
  happy path; prefilter ENFORCEMENT test (5 candidates seeded, `limit:2` → exactly 2 LLM calls, not 5);
  zod-repair-then-fail (2 always-malformed responses → exactly 1 repair retry → typed failure, batch
  doesn't crash); repair-succeeds-on-retry; budget-guard-blocks-with-recorded-failure; not_found;
  validation_failed for a not-yet-embedded profile.
- Integration, real Postgres (`packages/infrastructure/test/integration/match-score.repository.test.ts`):
  4/4 — persist+round-trip, upsert-NOT-append proof (exact row-count assertion), per-profile listing
  ordered by recency and correctly excluding another profile's scores, null-on-no-score.
- Integration, real Postgres+Redis+BullMQ (`apps/worker/test/integration/score-match.test.ts`): 1/1 —
  `matching.score_requested` → worker → REAL ANN prefilter query → REAL `prompts/match-score/v1.md`
  loaded off disk and rendered → guarded LLM call against a local HTTP stand-in → `match_scores`
  persisted with correct components/rationale/factsHash, `ai_invocations` billed once.
- API integration, real Postgres+Redis (`apps/api/test/integration/matching.test.ts`): 5/5 — auth
  required; 404 no-profile; 400 not-embedded; 202 + a REAL job actually landed in the REAL BullMQ
  queue with the right payload (not just an HTTP status check); `GET /profile/matches` empty-then-ranked
  (highest `overall` first).
- Docker: `docker build -f apps/worker/Dockerfile .` succeeds; `docker run` confirms
  `/app/prompts/match-score/v1.md` exists in the built image at the path `main.ts` resolves to.
- Full unit suite 391/391, full integration suite 106/106 (96 prior [033-037] + 4 repo + 1 worker
  e2e + 5 API), no regressions. `tsc --noEmit` clean across domain/application/contracts/infrastructure/api/worker.
  `eslint` clean. Boundary lint passes.
