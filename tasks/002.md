# 002 — Shared kernel: Result, IDs, DomainEvent

**Status:** DONE | **Milestone:** M2

## Objective
Zero-dependency primitives every other package builds on: typed `Result<T,E>` (expected failures are values, not exceptions), UUIDv7 ids, and the `DomainEvent` base.

## Files affected
- `packages/domain/src/shared/{result.ts,ids.ts,domain-event.ts,errors.ts}`

## Dependencies
001

## Acceptance criteria
- [ ] `Result` has `ok`/`err` constructors, type guards, `map`/`flatMap`/`unwrapOr`; exhaustive-checkable
- [ ] UUIDv7 generation is time-ordered (sorting ids sorts by creation)
- [ ] `DomainEvent` carries `eventType`, `aggregateType`, `aggregateId`, `occurredAt`, `payload`
- [ ] `packages/domain` has **zero** runtime dependencies (verified in package.json)

## Test plan
Unit (Vitest): Result algebra laws; UUIDv7 monotonicity across 10k generations.
