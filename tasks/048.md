# 048 — Known-ATS field maps (Greenhouse, Lever, Ashby, Workday)

**Status:** TODO | **Milestone:** M6

## Objective
Stage 1 of the three-stage field-mapping pipeline (`docs/05-playwright-design.md` §4): deterministic selector maps for the four named ATS hosted-form platforms, versioned per ATS, covered by recorded-fixture tests (not live sites — same anti-flake pattern M4's connector fixtures already established).

## Files affected
- `apps/browser-runner/src/ats-maps/{greenhouse,lever,ashby,workday}.ts` (new) — each exports a versioned selector map: canonical taxonomy field (name, email, phone, resume-upload, work-auth, EEO questions, ...) → CSS/ARIA selector, plus a `detect(page): boolean` (or DOM signature) to identify which adapter applies to a given form.
- `apps/browser-runner/src/ats-maps/taxonomy.ts` (new) — the canonical field taxonomy shared by this task, 049 (heuristics), and 050 (LLM mapper) — define this once here since 048 is the first consumer, 049/050 depend on it.
- `apps/browser-runner/test/fixtures/{greenhouse,lever,ashby,workday}/*.html` (new) — recorded/sanitized HTML snapshots of real hosted application forms (mirror M4's `packages/*/test/fixtures` recorded-fixture pattern for connectors).
- `apps/browser-runner/test/ats-maps.spec.ts` (new) — Playwright tests loading each fixture and asserting every selector in the map resolves to the expected field.

## Dependencies
- 047 (browser-runner scaffold + Playwright).

## Acceptance criteria
- Each of the 4 ATS maps resolves 100% of its declared taxonomy fields against its own fixture set (a map claiming to detect a field but failing against its own fixture is worse than not claiming it).
- `detect()` correctly identifies which adapter applies and returns false/no-match for the other 3 platforms' fixtures (no false-positive adapter selection).
- Sensitive-field policy from §4 is respected at the taxonomy level: EEO/demographic/veteran/disability fields are tagged `neverAutoFill: true` in the taxonomy, not silently mappable.

## Test plan
- Playwright fixture tests (this task's own `ats-maps.spec.ts`) — the primary proof, per `docs/05-playwright-design.md` §7's testing table ("Selector maps | Playwright tests against recorded HTML fixtures per ATS version").

## Status
DONE.

**Honest fixture-provenance note (important, not a footnote):** this sandbox has no network access
to scrape or record real employer job-application pages. The 4 ATS fixtures
(`test/fixtures/{greenhouse,lever,ashby,workday}/application-form.html`) are hand-built SYNTHETIC
HTML, structurally representative of each platform's well-documented public conventions
(Greenhouse's `#grnhse_app`/`#application-form`/underscored field ids; Lever's `data-qa`
attributes + `.posting-btn-submit`; Ashby's `data-testid` convention; Workday's `data-automation-id`
convention) — NOT scraped/recorded snapshots of any real branded page, and each fixture file says so
in its own header comment. This satisfies the task's own instruction ("not a copy of any real
platform's branded UI") but is a materially different provenance than "recorded fixture," disclosed
here rather than implied.

**Judgment call — taxonomy location:** the task file places the canonical taxonomy at
`apps/browser-runner/src/ats-maps/taxonomy.ts` with "task 050 depends on it." Taken literally that
would mean `packages/application` (framework-agnostic) importing from `apps/browser-runner` (an
app) — a dependency-rule inversion that also wouldn't resolve (pnpm's isolated `node_modules`).
Moved the real taxonomy to `packages/domain/src/apply/field-taxonomy.ts` (plain TS, zero deps —
genuinely shared vocabulary, not an HTTP/DTO shape) and made `apps/browser-runner/src/ats-maps/taxonomy.ts`
a re-export, so 048/049's own call sites needed no changes. Full reasoning in both files' doc
comments; see task 050's Status note for the rest of this boundary story.

**Files:** `apps/browser-runner/src/ats-maps/{taxonomy.ts (re-export), ats-map.ts,
greenhouse,lever,ashby,workday}.ts`, `index.ts` (`detectKnownAts`), `test/fixtures/{4 platforms}/application-form.html`,
`test/ats-maps.spec.ts`. `packages/domain/src/apply/field-taxonomy.ts` (new — the real taxonomy).
Each `AtsMap` also carries a `submitSelector` (added when task 053 needed it — the submit-button
selector per platform).

**Evidence** (Playwright, `apps/browser-runner/test/ats-maps.spec.ts`, real headless Chromium against the
fixtures — all 32 tests in the package pass, this file's own subset):
- Each of the 4 maps resolves 100% of its declared taxonomy fields against its own fixture (radio
  fields checked for `count > 0`, everything else for `count === 1`).
- `detect()` returns true for its own fixture and false against all 3 OTHER fixtures — 12
  cross-fixture pairs, zero false positives — plus a combined "exactly one adapter matches each
  fixture" sweep.
- Every sensitive (EEO) field each map declares is tagged `neverAutoFill: true`.
- Full unit suite 555/555, `tsc --noEmit` clean, `eslint` clean, boundary lint passes. `pnpm
  --filter @careerpilot/browser-runner exec playwright test`: 32/32.
