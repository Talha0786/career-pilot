# 004 — Contracts package (zod DTOs)

**Status:** DONE | **Milestone:** M2

## Objective
Single source of truth for wire shapes, shared by API and web so they cannot drift.

## Files affected
- `packages/contracts/src/{auth.ts,jobs.ts,board.ts,errors.ts,index.ts}`

## Dependencies
001

## Acceptance criteria
- [ ] zod schemas for every M2 endpoint (design §6), request and response
- [ ] Error shape is `application/problem+json` with stable machine-readable codes
- [ ] Types are inferred from schemas (`z.infer`) — no hand-written duplicates
- [ ] Web imports the same package as the API

## Test plan
Unit: parse/reject fixtures per schema. CI: schema-drift check fails if a route's declared schema diverges from contracts.
