# 039 — Resume/cover-letter tailoring pipeline

**Status:** DONE | **Milestone:** M5

## Objective
Implement the highest-stakes pipeline in the system (`docs/06-agent-design.md` §3-4): given a profile's fact list (037) + a target job description, produce a tailored `StructuredDoc` (reusing M3's `ResumeDocumentContent`/`CoverLetterDocumentContent` — do not invent a new structured-doc model) constrained to *rephrase, reorder, emphasize, quantify only from provided facts* — never introduce new entities/employers/dates/titles/credentials/metrics.

## Files affected
- `prompts/tailor-resume/v1.md`, `prompts/tailor-cover-letter/v1.md` (fill in real content — stubbed in task 034) — system prompt must explicitly enumerate the fact-list constraint from docs/06 §4 point 1, and require the LLM to cite which fact ID(s) support each output bullet/sentence (this citation is what claim verification in 040 checks against — design the output schema now so 040 doesn't need a breaking schema change).
- `packages/contracts/src/tailoring.ts` (new) — `TailoringRequestSchema { profileId, jobPostingId, kind: 'resume'|'cover_letter' }`, `TailoringResultSchema` wrapping `ResumeDocumentContentSchema`/`CoverLetterDocumentContentSchema` (from `packages/contracts/src/documents.ts`) plus a per-bullet `supportingFactIds: string[]` field threaded through the structured content.
- `packages/domain/src/documents/document-content.ts` — extend entry/bullet shapes with an optional `supportingFactIds` field if not already extensible (check before assuming a breaking change is needed — prefer additive).
- `packages/application/src/tailoring/commands/tailor-document.ts` (new) — orchestrates: `compileFactList` (037) → load prompt (034) → guarded `llm.complete()` → zod-validate against `TailoringResultSchema` (one repair attempt, ADR-006 rule) → `Document.addVersion({source: 'generated', content, profileFactsHash, generationJobId})`.
- `apps/worker/src/main.ts` — worker handler for tailoring jobs (async, since this is a slow/expensive `large` model-tier call per docs/06 §3).
- `apps/api/src/routes/documents.ts` (extend) or new `apps/api/src/routes/tailoring.ts` — `POST /documents/tailor` (or match existing route-naming convention in that file) enqueues the job; a status-poll or WS update (reuse the M2 WS pattern already used for board live-updates) notifies the client when done.
- Tests: unit (fake LLM, assert facts-only constraint is *structurally* enforced — i.e. every bullet's `supportingFactIds` resolves to a real fact in the input fact list, reject/flag any that don't), integration (real Postgres, `Document.addVersion` persistence), worker integration test.

## Dependencies
- 033 (CostEstimator — `large` tier calls are the most expensive in the system, budget accuracy matters most here).
- 034 (PromptStore).
- 037 (fact list — the tailoring prompt's fact base).

## Acceptance criteria
- Output `ResumeDocumentContent`/`CoverLetterDocumentContent` validates against the existing M3 schema (reuse, not fork).
- Every generated bullet carries `supportingFactIds` referencing real fact IDs from the input fact list — this is checked structurally in code (not just trusted from the LLM), and any bullet with an empty or invalid `supportingFactIds` is treated as unsupported (feeds directly into 040's claim verification, but do the structural check here too as a first cheap gate before the expensive adversarial LLM pass).
- Malformed LLM output → one repair attempt → typed failure with raw output stored for triage (ADR-006, same rule as 038).
- `DocumentVersion.profileFactsHash` is populated from the exact fact list used (037's determinism guarantee makes this meaningful).

## Test plan
- Unit: orchestration with fake LLM returning valid + invalid (hallucinated fact ID, missing supportingFactIds) fixtures — assert the structural gate catches the invalid ones.
- Integration: real Postgres, full command → `DocumentVersion` persisted with correct `source: 'generated'` and `profileFactsHash`.

## Status
DONE.

**Structured-content model:** reused `ResumeDocumentContent`/`CoverLetterDocumentContent` verbatim,
per instruction — did NOT fork a new model. Threaded per-bullet `supportingFactIds` through via
NEW, ADDITIVE, OPTIONAL parallel fields (`ResumeEntry.bulletFacts`, `CoverLetterDocumentContent.paragraphFacts`,
both `SupportedText[]`, same order/text as the existing `bullets`/`bodyParagraphs` arrays) rather
than changing `bullets: string[]` to `{text, supportingFactIds}[]` — that would have broken the
task 024 renderer (golden-file tested) and every `imported`/`edited` version. Verified: renderer
unit tests (27) still pass unchanged after this change.

**Contact info gap (documented, not silently papered over):** neither `User` nor `CareerProfile`
has a "display name" field anywhere in this data model. `contact.email` comes from the real
`User.email` (never the LLM); `contact.name` is heuristically derived from the email local part
(e.g. `jane.doe@x.com` -> "Jane Doe") since the schema requires a non-empty name and the LLM is
never asked to supply one (an actual person's name is exactly the kind of fact that must never be
LLM-invented). Flagged in `tailor-document.ts`'s doc comment as a real gap for a human to correct
in the diff-review UI (task 041) or a follow-up task adding a proper name field.

**Structural gate (task's own explicit two-gate design):** every bullet/paragraph's
`supportingFactIds` is checked in CODE against the real fact-id set from `compileFactList` (task
037) — never trusted from the LLM. Ids not in the set are stripped before persistence (verified:
a hallucinated `"F99"` citation never survives into the stored `DocumentVersion.content`, only
`[]` does) and the bullet/paragraph text is recorded in `structurallyUnsupported` for the caller.
This task's gate does NOT block persistence or retry on its own — per the acceptance criterion,
it's "a first cheap gate before the expensive adversarial LLM pass" that task 040 builds on to
add the actual retry-then-needs_human decision.

**Route/queue design:** `POST /documents/:id/tailor` (matches task 022's existing `/documents/:id/...`
convention) enqueues `tailoring.document_requested`; `apps/worker`'s new `createTailorDocumentWorker`
does the actual `large`-tier generation (async, same "never block an HTTP request on this"
reasoning task 038 established one tier down). WS event `document.tailored` added alongside the
existing `job.embedded` channel (`ConnectionHub.sendToUser` broadened to a union type) as the
"status-poll or WS update" the task asked for.

**Evidence:**
- Unit (`packages/application/test/unit/tailor-document.test.ts`, fakes only): 9/9 — schema
  conformance + correct `profileFactsHash`; the EMPTY-supportingFactIds structural-gate case; the
  HALLUCINATED-fact-id structural-gate case (asserts the invalid id is stripped from what's
  persisted, not just flagged); cover-letter transitional-paragraph-may-be-empty case;
  malformed-JSON-repair-then-fail (nothing persisted on failure); repair-succeeds; not_found;
  validation_failed (no facts yet); and the real-email-derived-contact-info case.
- Integration, real Postgres+Redis+BullMQ (`apps/worker/test/integration/tailor-document.test.ts`):
  1/1 — full pipeline against REAL `compileFactList` output and the REAL `prompts/tailor-resume/v1.md`
  loaded off disk, asserting the LLM's cited fact id (`"F1"`) matches what the real fact compiler
  actually produced (not just "some string round-trips") and `ai_invocations` billed once under
  `context: 'tailoring'`.
- API integration, real Postgres+Redis (`apps/api/test/integration/tailoring.test.ts`): 5/5 — auth
  required; 404 unowned/missing document; 400 for a non-tailorable `other`-kind document; 202 + a
  REAL BullMQ job verified in the REAL queue with the right payload; 404 with no active profile.
- Full unit suite 400/400 (391 prior + 9). Full integration suite 112/112 (106 prior + 1 worker
  e2e + 5 API), no regressions.
- `tsc --noEmit` clean across domain/application/contracts/infrastructure/api/worker. `eslint`
  clean (two real dead-code/unused-import warnings found and fixed, not just silenced). Boundary
  lint passes — `packages/application/src/tailoring/**` has no `@careerpilot/contracts` import
  (own zod validator, same posture as `score-match.ts`).
