# 003 — Domain: User, JobPosting, Application aggregates

**Status:** DONE | **Milestone:** M2

## Objective
Model the M2 slice's domain with enforced invariants — not anemic data bags.

## Files affected
- `packages/domain/src/profile/user.ts`
- `packages/domain/src/discovery/{job-posting.ts,events.ts,value-objects.ts}`
- `packages/domain/src/pipeline/{application.ts,stage.ts,events.ts}`

## Dependencies
002

## Acceptance criteria
- [ ] `Email`, `JobUrl` value objects reject invalid input at construction (`Result`, not throw)
- [ ] `JobPosting.createManual()` normalizes and emits `JobPosted`; `embeddingStatus` starts `pending`
- [ ] `JobPosting.attachEmbedding()` is **idempotent** — re-applying the same model's embedding is a no-op, not an error
- [ ] `Application.transitionTo()` rejects illegal stage moves (e.g. `rejected → applied`) and appends a transition
- [ ] No aggregate imports anything outside `domain`

## Test plan
Unit: invariant tests per aggregate; a stage-transition table test covering every legal and illegal pair.
