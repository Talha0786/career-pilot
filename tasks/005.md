# 005 — Application ports + use cases

**Status:** TODO | **Milestone:** M2

## Objective
The use cases for the slice, written against ports only — no infrastructure imports anywhere in this package.

## Files affected
- `packages/application/src/ports/*` (repositories, `LlmPort`, `QueuePort`, `UnitOfWork`, `ClockPort`, `HasherPort`)
- `packages/application/src/auth/{register,login}.ts`
- `packages/application/src/discovery/commands/{create-manual-job,embed-job-posting}.ts`
- `packages/application/src/pipeline/queries/get-board.ts`

## Dependencies
003, 004

## Acceptance criteria
- [ ] Every use case takes an `actor` and asserts ownership; repos expose no unscoped `findById`
- [ ] `createManualJob` persists the aggregate **and** the outbox event in one `UnitOfWork` transaction
- [ ] `embedJobPosting` is idempotent by `(jobPostingId, model)`
- [ ] Expected failures are `Result` errors (`InvalidCredentials`, `BudgetExceeded`, `NotFound`), never thrown
- [ ] All tests here use **fake** ports; zero infrastructure

## Test plan
Unit with in-memory fakes: happy path, ownership violation → `Forbidden`, duplicate embed → single effect.
