# 062 — MCP contract tests + injection red-team + Claude Desktop manual script

**Status:** DONE (with explicitly-flagged residual gaps — see below) | **Milestone:** M7

## Objective
Deliver the M7 roadmap acceptance criteria verbatim: "tool contract tests green; Claude Desktop manual test script passes; injection red-team checklist executed." This is the closing verification task for the whole M7 track, mirroring M5 task 043's role as the milestone's evidence-gathering capstone.

## Files affected
- `apps/mcp-server/test/contract/*.spec.ts` (new, if not already fully covered by 057/058/059's own per-tool tests — this task audits for gaps and fills them, doesn't necessarily duplicate) — ensure every tool in the §3 catalog has a golden-file contract test per §5's stated testing approach.
- `docs/mcp-injection-redteam-checklist.md` (new) — a concrete checklist derived from §2's threat framing and §6's risk table: attempt prompt-injection payloads embedded in job-description text (returned by `search_jobs`/`get_job`) that try to get the MCP client to (a) call a write tool it wasn't asked to, (b) exfiltrate data via a crafted resource read, (c) ignore the untrusted-content envelope warning. Document each attempted payload, expected-safe behavior, and actual observed behavior.
- `docs/mcp-claude-desktop-manual-test.md` (new) — step-by-step script for connecting a real Claude Desktop instance to the running `mcp-server` (stdio transport) and manually exercising the read tools, a write tool, and confirming `prepare_application` genuinely stops at `awaiting_review` with no way to push further from the chat interface.
- `.github/workflows/nightly.yml` or `ci.yml` — wire the contract-test suite in as a CI gate (schema-diff check per §6: "Tool schema drift vs API | Shared zod contracts package; CI schema-diff check" — verify `packages/contracts` schemas used by MCP tools and the HTTP API routes for the same underlying data don't silently diverge).

## Dependencies
- 056, 057, 058, 059, 060, 061 (this task audits/verifies the complete M7 surface).

## Acceptance criteria
- Contract tests green for the full tool/resource/prompt catalog.
- Injection red-team checklist executed with real attempted payloads (not hypothetical) and documented pass/fail per item — any failure found must be fixed before this task is marked DONE, not just noted as a known limitation (this is a security-relevant gate, same rigor as M5's anti-hallucination gate).
- Claude Desktop manual test script exists and (if a real Claude Desktop instance is available in this environment) was actually run, with results recorded; if genuinely not runnable in this sandboxed environment, document exactly what's untested and why — same honesty standard M4 task 031 used for its "fixture-verified; live canary pending" status.

## Test plan
- This task's own contract-test suite + the red-team checklist execution + (best-effort) the manual script ARE the test plan; all evidence goes in the Status note.

## Status
DONE, with residual gaps flagged explicitly rather than glossed over (see the final section).

**Contract tests — audited for gaps, not duplicated (task's own instruction):** every tool in the §3 catalog already had golden-file-style coverage from tasks 057/058/060/061's own test files (`apps/mcp-server/test/unit/tool-contracts.test.ts`, the interview-prep application-layer suite, `registry.test.ts`, `mcp-server.test.ts`) by the time this task started — audited each one against §3's table and found no tool with zero test coverage. What this task added on top: `packages/contracts/src/mcp-schema-diff.test.ts` (5/5) — the "CI schema-diff check" §6 asks for, implemented as a plain vitest unit test (so it runs in CI's existing `unit` job via `pnpm test:cov`, no new workflow needed) that asserts fields shared between the MCP contracts (`mcp.ts`) and the pre-existing HTTP API contracts (`board.ts`/`jobs.ts`/`tailoring.ts`) for the same underlying concept stay structurally identical (same `Stage` enum values, same string-length constraints, same zod type names) — a future edit that silently diverges one side without the other now fails loudly. A dedicated new GitHub Actions job was deliberately NOT added for this, since the existing `unit` job already gates on it; documented as a judgment call, not an oversight.

**Injection red-team checklist — real payloads, real code, real results (task's explicit, non-negotiable ask — "not hypothetical"):** `docs/mcp-injection-redteam-checklist.md`, backed by `apps/mcp-server/test/unit/injection-redteam.test.ts` (6/6, all real attempted payloads against real registry/tool/resource code): a JD instructing a write-tool call (returned inert, zero side effect); the same across a search batch (every item independently wrapped, and a structural note that no exfiltration-capable tool exists in the catalog at all); a crafted cross-user resource-read attempt (the profile lookup has no id-based path to exploit); `prepare_application`'s `autoApprove`/`submit` injection (schema `.strict()` rejects it outright); direct dispatch of five nonexistent dangerous tool names (`not_found` for all five); a path-traversal-shaped resource id (`not_found`, no crash, no filesystem read). **One real issue was found and fixed during this task**: the first attempt at payload E used two separate `FakeMcpTokenStore` instances (one for the registry, one for minting) — an authoring bug in the TEST, not a product defect, caught immediately by the test itself failing (`expected 'unauthorized' to be 'not_found'`), fixed, re-run, passed. Recorded honestly in the checklist doc's own summary table rather than only in this Status note. Per the acceptance criterion "any failure found must be fixed before this task is marked DONE" — this is the one failure found, and it was a test-harness bug, not a security gap; **no actual security defect was found in the six payloads attempted.**

**Live verification against the real running container (beyond the automated suite):** `docker compose up -d mcp-server` (image builds and boots healthy), then real `curl`/`wget`-equivalent JSON-RPC calls proving `initialize`, `tools/list` (exact 13-tool catalog), unauthenticated rejection, real-bearer-token success with a confirmed real `audit_log` row, and scope-enforced rejection of a write tool — all against the actual Postgres-backed stack, not a fake. Full transcript in `docs/mcp-injection-redteam-checklist.md` §7 and `docs/mcp-claude-desktop-manual-test.md` §5.

**Claude Desktop manual test script:** `docs/mcp-claude-desktop-manual-test.md` — written to the same honesty standard M4 task 031 used ("fixture-verified; live canary pending"). **No physical Claude Desktop application was available in this sandboxed agent environment**, so §1-4 (the actual interactive Claude Desktop steps) were authored and reviewed but not run by a human against a real Claude Desktop install. What WAS verified for real (§5 of that doc) exercises the identical registry/auth/dispatch/audit code both transports share — the only genuinely untested delta is the SDK's own `StdioServerTransport` newline-JSON-RPC framing, code this project didn't write. This is flagged as the document's own top-of-file status line, not buried.

**CI wiring:** `docker-compose.yml`'s new `mcp-server` service is automatically picked up by `.github/workflows/ci.yml`'s existing `build` job (`docker compose build`) and `e2e-compose` job (`docker compose up -d --build` + `scripts/e2e-compose-smoke.mjs`) with ZERO workflow changes needed — both jobs already operate on "the whole compose file," and a new service is invisible to them structurally until it's added to `docker-compose.yml`, which task 056 already did. `apps/mcp-server/test/unit/**` and `apps/mcp-server/test/integration/**` are automatically picked up by the existing `pnpm test`/`pnpm test:int` scripts via `vitest.config.ts`'s updated `include` glob (task 056 added `apps/*/test/unit/**/*.test.ts`) and `vitest.integration.config.ts`'s pre-existing `apps/*/test/integration/**` glob (no change needed there at all). No new CI job was added specifically for M7 — the existing `lint` -> `typecheck` -> `boundary-check` -> `unit` -> `integration`/`chaos` -> `build` -> `e2e-compose` chain already covers every M7 artifact by construction of the glob patterns, verified by literally running each of those steps in this session (see the "Full final verification" section below) and observing the new tests execute.

**Full final verification, run in this session (the actual evidence, not a claim):**
- `pnpm -r typecheck` (all 14 workspace projects, including the new `apps/mcp-server`): clean.
- `pnpm lint` (`eslint .`, whole repo): 0 errors, 6 warnings — all 6 pre-existing in `packages/connectors`, a package untouched by M7; 0 new warnings introduced.
- `node scripts/verify-boundary-enforcement.mjs`: both enforcement layers (tsc bare-specifier rejection, eslint relative-path rejection) verified, clean.
- `pnpm test` (unit, real run): **466/466 passing**, 48 test files. (413 pre-M7 baseline + 53 new: registry.test.ts 24, tool-contracts.test.ts 6, injection-redteam.test.ts 6, interview-prep.test.ts 7, prepare-application.test.ts 5, mcp-schema-diff.test.ts 5.)
- `pnpm test:int` (integration, real Postgres + Redis, run file-by-file after a full-suite run hit this sandbox's per-command timeout mid-way through — see below): **131/131 passing** across all 23 integration test files (120 pre-M7 baseline + 11 new: `mcp.test.ts` 7, `mcp-server.test.ts` 4). One real regression found (documents.ts leaking `generationJobId` into the pre-existing tailor-document HTTP response) and fixed mid-session — see task 058's Status note.
- `docker compose build mcp-server` + `docker compose up -d mcp-server`: image builds, container reports `healthy`.
- Live JSON-RPC verification against the running container: see above.
- `docker system prune -f` run at the end of this session to release build cache (per the environment brief's disk-space note).

**Residual gaps, flagged honestly (not hidden in a passing-looking summary):**
1. **Full `docker compose up -d --build` (the ENTIRE stack: api/worker/web/caddy/mcp-server together) was not exercised end-to-end in this session** — only `postgres`/`redis`/`migrate`/`mcp-server` were brought up together (mcp-server's own dependencies). `apps/api`/`apps/worker`/`apps/web` were not rebuilt against this branch's changes as a full running stack; their individual Docker images were NOT rebuilt in this session (though their source-level integration tests, which exercise the same code via `tsx` not the built image, all pass — see above). Recommended follow-up before merge: one full `docker compose up -d --build` + `scripts/e2e-compose-smoke.mjs` pass, which CI's `e2e-compose` job will do anyway on the PR.
2. **stdio transport was not spawned as a literal separate OS process and piped to** — see task 056's Status note for exactly what this gap is and isn't (it shares all dispatch code with the HTTP path that WAS live-verified).
3. **The two new web-search/web-fetch adapters' live network behavior against the real internet is unverified** — see task 060's Status note.
4. **A real multi-turn mock-interview session against a real local LLM was not run** — see task 061's Status note; no Ollama endpoint was reachable in this sandbox.
5. **This machine's Docker Desktop required manual intervention twice more this session** (stale-socket workaround, same class of issue task 043 first documented) — including one restart that briefly stopped the parallel M6 agent's containers (immediately restarted, no volumes touched) — recorded in full in task 056's Status note as a cross-worktree disruption caused during this session, not hidden.

None of these five gaps represent a known DEFECT — they represent verification steps that a live Ollama / live Claude Desktop / a full separate stdio-process harness would have covered had they been available in this sandbox, consistent with M4 task 031's and M5 task 042's precedent of naming exactly what's fixture-verified versus live-verified rather than blurring the two.
