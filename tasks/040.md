# 040 — Claim verification pass (anti-hallucination gate)

**Status:** DONE | **Milestone:** M5

## Objective
Implement `docs/06-agent-design.md` §4 point 2-3: a **separate call, different prompt, adversarial framing** that extracts every factual claim from a tailored draft and maps each to a fact ID or marks it `UNSUPPORTED`; unsupported claims trigger ≤2 targeted regeneration retries, then `needs_human` with flagged claims. This is the mandatory safety gate — task 039's structural `supportingFactIds` check is a cheap first pass, this is the real adversarial check the roadmap's acceptance criteria and eval harness (042) are gated on ("regression gate: 0 unsupported claims surviving verification").

## Files affected
- `prompts/verify-claims/v1.md` (fill in real content — stubbed in 034) — adversarial framing per docs/06 §4: given the fact list + the generated draft, extract every claim and independently re-derive its fact mapping (must NOT simply echo back the draft's own `supportingFactIds` claims — that would make this pass useless as an adversarial check; it must re-derive from the draft text + fact list only).
- `packages/contracts/src/tailoring.ts` (extend) — `ClaimAuditSchema { claims: [{ text: string, factId: string|null /* null = UNSUPPORTED */, confidence: number }] }`.
- `packages/application/src/tailoring/commands/verify-claims.ts` (new) — guarded `llm.complete()` call, zod-validate `ClaimAuditSchema`.
- `packages/application/src/tailoring/commands/tailor-document.ts` (from 039, extend) — after generation, run `verify-claims`; if any claim is `UNSUPPORTED`, retry generation with the unsupported claims fed back into the prompt as "avoid this" hints (≤2 attempts); if still unresolved after retries, persist the document version with a `needs_human: true` flag (extend `DocumentVersion` if it doesn't already have a review-status field) and the flagged claims attached for the review UI (041) to render.
- `packages/domain/src/documents/document.ts` / `document-version.ts` — add whatever minimal state is needed to represent "generated but pending human review with N flagged claims" if not already expressible.
- Tests: unit (fake LLM sequences: clean-first-try, unsupported-then-clean-on-retry, unsupported-through-all-retries → needs_human), integration (real Postgres, persisted `needs_human` state + flagged claims retrievable by the API for the review UI).

## Dependencies
- 039 (tailoring pipeline produces the draft this verifies).
- 034 (PromptStore).

## Acceptance criteria
- Claim verification is a genuinely separate LLM call with its own prompt file, not a reuse/rerun of the tailoring prompt.
- 0-unsupported-claims is enforced as a hard gate before a document version can be marked ready for export — `needs_human: true` documents are not exportable until a human resolves the flagged claims (export-blocking check enforced in code, test it explicitly — don't just trust the UI to hide the button).
- Retry budget is exactly ≤2 regeneration attempts, then stop and flag — proven with a fake-LLM test that always returns unsupported claims (must terminate after 2 retries, not loop or hang).

## Test plan
- Unit: the three retry-outcome paths above with a fake LLM.
- Integration: real Postgres — `needs_human` documents are queryable, export route (extend/verify against task 022's document routes) rejects export of a `needs_human: true` version with a clear error, not a silent no-op.

## Status
DONE. Treated as one of the two highest-risk tasks in M5 (per the tracking instructions) — extra
care taken on the retry-termination proof and the export-blocking gate specifically.

**Genuinely separate call/prompt, adversarial framing:** `prompts/verify-claims/v1.md` (task 034
stub, filled in here) is a distinct prompt file from `tailor-resume`/`tailor-cover-letter`, framed
as an adversarial auditor that must INDEPENDENTLY re-derive each claim's fact mapping. The draft
text handed to it is built from `content.bullets`/`content.bodyParagraphs` (plain strings) —
never `bulletFacts`/`paragraphFacts` (the ids) — so there is no code path by which the auditor
could see the draft's own citations, structurally enforcing the "must not echo back" requirement,
not just a prompt instruction. Judgment call: tightened the prompt to make omitting
non-factual/transitional sentences from `claims` MANDATORY (not "may omit, or include with
factId:null") — the optional phrasing was ambiguous about whether a transitional sentence with
`factId:null` should count as "unsupported"; making omission mandatory removes the ambiguity so
every entry in `claims` is unambiguously a real assertion needing a real fact.

**Domain state:** `DocumentVersion` gained `needsHumanReview: boolean` + `flaggedClaims:
FlaggedClaim[] | null` (additive) and an `isExportable()` method. `Document.addVersion` threads
both through. Migration `0006_claim_verification.sql` adds the columns (default `false`/`null` —
every pre-existing version is retroactively "nothing to review").

**Retry loop (`tailor-document.ts`, extended from 039):** generate -> extract claim texts (ids
stripped) -> adversarial verify -> if any claim's `factId` is `null` or not in the real fact-id
set, regenerate with those exact claim texts fed back as an explicit "could not verify, do not
repeat" hint -> loop, bounded by a fixed `for` loop at exactly `MAX_REGENERATION_RETRIES = 2`
(3 total generation+verify cycles: 1 initial + 2 retries) -> if still unsupported after the last
attempt, persist with `needsHumanReview: true` and the flagged claims attached. A provider/budget
failure during verification is propagated as a real error, never silently treated as "clean" (a
deliberate anti-footgun: silence must never mean safety in this pipeline).

**Export-blocking gate:** enforced in `render-document.ts` (the one and only path that produces a
downloadable artifact) — `!version.isExportable()` returns a `conflict` error before the renderer
is ever called. Tested explicitly at both the unit level (fake renderer, asserts zero calls) and
the integration level (real Postgres-persisted `needsHumanReview:true` row, real
`makeRenderDocumentUseCase`, real rejection).

**Evidence:**
- Unit (`packages/application/test/unit/verify-claims.test.ts`): 4/4 — separate-prompt load +
  schema validation, explicit UNSUPPORTED (`factId:null`) case, malformed-JSON repair-then-fail,
  typed error on missing prompt file.
- Unit (`packages/application/test/unit/tailor-document.test.ts`, rewritten for the new call
  sequence): 12/12 in the original describe block (structural gate + generation repair + not_found
  etc., re-verified against the new flow) + 3/3 in a new describe block: (a) unsupported-on-attempt-1
  -> hint fed back -> clean-on-attempt-2 -> succeeds, asserting the SECOND generation prompt
  actually contains the fed-back claim text and "could not verify"; (b) **the critical proof** — an
  audit that is ALWAYS unsupported terminates at EXACTLY 6 LLM calls (3 generations + 3 audits,
  never more) and persists `needsHumanReview:true` with the right `flaggedClaims`, not a
  hang/crash/loop; (c) a genuine verify-claims provider failure surfaces as an error, nothing
  persisted, never defaults to "clean".
- Unit (`packages/application/test/unit/render-document.test.ts`, extended): the export-blocking
  gate — a `needsHumanReview:true` version is rejected with `code: 'conflict'`, zero renderer
  calls (not a silent no-op).
- Integration, real Postgres+Redis+BullMQ (`apps/worker/test/integration/tailor-document.test.ts`,
  extended): the pre-existing happy-path test updated for the new two-completion-type fake server
  (generation vs. audit, distinguished by prompt content) and now also asserts `needsHumanReview:
  false` + 2 real `ai_invocations` rows. NEW test: a fake model that ALWAYS disagrees drives the
  full retry loop through the REAL worker/queue/Postgres stack — exactly 6 real HTTP completions,
  exactly 1 persisted `document_versions` row (not one per retry), `needsHumanReview:true` and
  `flaggedClaims` retrieved back through the real `DrizzleDocumentRepository` (not asserted against
  an in-memory write-side object), and the real `renderDocument` use case rejecting export of that
  exact persisted row.
- Full unit suite 408/408 (400 prior + 4 verify-claims + adjustments to existing tailor-document/
  render-document tests). Full integration suite 113/113 (112 prior + 1 new needs_human/export-gate
  test in the extended tailor-document.test.ts) — run twice for reliability since the first full
  run hit one transient failure unrelated to this task's changes (a redis.quit() timing issue in
  an unrelated pre-existing test during a very long ~630s run); the second full run came back clean
  at 113/113, and the two task-040-specific integration tests were also independently verified
  passing on their own beforehand.
- `tsc --noEmit` clean across domain/application/contracts/infrastructure/api/worker. `eslint`
  clean. Boundary lint passes.
