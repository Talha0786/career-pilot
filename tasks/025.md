# 025 — Web UI: profile editor, resume-import confirm flow, document history

**Status:** TODO | **Milestone:** M3

## Objective
User-facing surface for everything built in 019-024, using `@careerpilot/ui` (task 018).

## Files affected
- `apps/web/app/profile/page.tsx` (section-by-section editor)
- `apps/web/app/profile/import/page.tsx` (upload → review/edit parsed draft → confirm)
- `apps/web/app/documents/page.tsx` (list + version history, staleness badge driven by `profileFactsHash` mismatch)
- `apps/web/lib/api/{profile,documents}.ts` (typed client calls against task 022's routes)

## Dependencies
022, 023, 024, 012, 018

## Acceptance criteria
- [ ] Import confirm screen shows parsed fields with confidence/diff markers so low-confidence fields are obviously reviewable (supports the accuracy story from 023 — errors must be cheap to catch, per roadmap risk mitigation)
- [ ] Document list shows a visible "stale" indicator when a document's `profile_facts_hash` no longer matches the current profile
- [ ] No client-side state library added beyond what 012/018 already established

## Test plan
Manual verification in the browser (per project convention for UI work — start the dev server, exercise upload→confirm, edit a profile section, verify staleness badge appears after a profile edit). Playwright e2e for the golden path only (upload a fixture resume → confirm → see it on `/profile`).
