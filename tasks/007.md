# 007 — Repositories + Testcontainers integration tests

**Status:** DONE | **Milestone:** M2

## Objective
Drizzle adapters implementing the application's repository ports, plus a `UnitOfWork` giving real transactional guarantees.

## Files affected
- `packages/infrastructure/src/db/repositories/{user,job-posting,application,outbox}.repository.ts`
- `packages/infrastructure/src/db/unit-of-work.ts`
- `packages/infrastructure/test/setup-postgres.ts`

## Dependencies
005, 006

## Acceptance criteria
- [ ] `UnitOfWork.withTransaction()` rolls back **all** writes on error, including the outbox row
- [ ] Repos are ownership-scoped (no unscoped reads)
- [ ] Round-trip fidelity: domain → row → domain preserves value objects and enums
- [ ] Integration tests run against **real Postgres** via Testcontainers — never a mock, never SQLite

## Test plan
Integration: CRUD round-trips; a forced error inside `withTransaction` leaves zero rows in both the aggregate table and `outbox` (this is the test that proves the dual-write is gone).
