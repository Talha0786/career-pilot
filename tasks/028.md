# 028 — Class A connectors: Greenhouse, Lever, Ashby, USAJobs, RSS, manual

**Status:** TODO | **Milestone:** M4

## Objective
The zero-legal-risk, official-API connectors (ADR-004 Class A). Each is a small adapter implementing `ConnectorPort` against a real public API/feed shape.

## Files affected
- `packages/connectors/class-a/{greenhouse,lever,ashby,usajobs,rss,manual}/index.ts`
- `packages/connectors/class-a/*/test/fixtures/*.json` (recorded real API responses — roadmap's mitigation for "third-party API instability": CI runs against recorded fixtures, not live calls)
- `packages/connectors/class-a/*/*.test.ts` (each connector run through task 026's contract test-kit + its own fixture-based normalization test)

## Dependencies
026, 027

## Acceptance criteria
- [ ] All 6 connectors pass the shared contract test-kit
- [ ] Each connector's fixture-based test asserts correct normalization to canonical `RawJob` (title/company/location/salary/postedAt mapped correctly from that API's actual response shape)
- [ ] `manual` connector is the existing paste-a-job path (task 011) re-expressed as a `ConnectorPort` implementation — not new functionality, just conforming the existing one to the new interface
- [ ] No live network calls in the default test run (fixtures only); a separate nightly job (mirroring task 014's Ollama nightly pattern) hits live endpoints as a canary

## Test plan
Unit (fixture-based, per connector) in the default CI run. Nightly workflow addition: live canary against each real API, non-blocking (report-only, matching the roadmap's stated mitigation).
