# 011 — API: Fastify, auth, jobs, board, WebSocket

**Status:** TODO | **Milestone:** M2

## Objective
The HTTP/WS surface — thin routes over use cases, with the composition root as the only place adapters meet ports.

## Files affected
- `apps/api/src/{main.ts,plugins/{auth,errors,logging},routes/{auth,jobs,applications,board,admin,ws}}`

## Dependencies
005, 007, 009

## Acceptance criteria
- [ ] Routes contain **no** business logic — parse DTO, call use case, map `Result` to HTTP
- [ ] `POST /jobs` returns **202** (`embeddingStatus: pending`) — forces the UI to handle async from day one
- [ ] Argon2id passwords; httpOnly + SameSite=Lax + Secure session cookie; sessions in Redis
- [ ] Central error handler → `problem+json`; stack traces to logs only, never to clients
- [ ] `/ws` fans out `job.embedded` to the owning user **only**
- [ ] `/healthz`, `/readyz` (checks PG + Redis), `/admin/status` (queue depth, outbox backlog, MTD spend)
- [ ] Rate limiting on auth routes

## Test plan
Integration (Fastify inject + Testcontainers): auth flows; ownership — user A cannot read user B's job (**403**); validation errors return `problem+json`.
