# 035 — Profile embedding use case

**Status:** DONE | **Milestone:** M5

## Objective
`CareerProfile` already has `embeddingStatus/embeddingModel/embedding/embeddedFactsHash` fields and an `isEmbeddingStale` getter (task 019), but nothing ever calls `llm.embed()` for a profile — only `embed-job-posting.ts` exists. Add the analogous use case and wire it into the worker so profiles get embedded (and re-embedded on `factsHash` drift) the same way job postings already do.

## Files affected
- `packages/application/src/profile/commands/embed-career-profile.ts` (new) — mirror `packages/application/src/discovery/commands/embed-job-posting.ts`'s shape exactly (idempotent by model, guarded LLM call, persists via `ProfileRepository`).
- `packages/domain/src/profile/career-profile.ts` — reuse existing `attachEmbedding()`/`isEmbeddingStale` (should need no changes if task 019 built them correctly; verify and only touch if there's a real gap).
- Event wiring: profile creation/update needs to enqueue an embed job the same way `job-posted.handler.ts` does for job postings — find the equivalent profile domain event (check `packages/domain/src/profile` for a `ProfileUpdated`/`ProfileSectionsChanged` event; if none exists, add one following the existing `DomainEvent` pattern and wire it through the outbox, same transactional-outbox discipline as ADR-007).
- `apps/worker/src/main.ts` — register the new handler alongside the existing job-posting embed handler.
- `packages/application/test/unit/embed-career-profile.test.ts` (new).
- `packages/infrastructure/test/integration/` — add a real-Postgres integration test proving profile embed idempotency (re-running on an unchanged `factsHash` is a no-op; changed `factsHash` triggers re-embed), mirroring whatever integration test already covers `embed-job-posting`.

## Dependencies
None (uses only already-DONE M2/M3 machinery: `GuardedLlmPort`, `CareerProfile`, outbox).

## Acceptance criteria
- Creating/updating a profile eventually results in `embeddingStatus: 'ready'` with a populated `embedding` vector, via the same outbox → worker → guarded-LLM path as job postings.
- Re-embedding is idempotent: unchanged `factsHash` does not re-trigger a paid embed call.
- `isEmbeddingStale` flips correctly when sections change (should already work from task 019 — this task's test plan must prove it end-to-end through the new use case, not just re-assert the M3 unit test).

## Test plan
- Unit: use case with a fake `LlmPort` + fake `ProfileRepository`, embed happy path, idempotent-skip path.
- Integration: real Postgres, real outbox relay, real worker handler — profile create → embed job flows through and lands `embeddingStatus: 'ready'`.

## Status
DONE — `makeEmbedCareerProfileUseCase` in `packages/application/src/profile/commands/embed-career-profile.ts`
mirrors `embed-job-posting.ts` exactly. `CareerProfile.attachEmbedding` already took `(vector, model,
factsHash)` from task 019 — verified, no domain change needed there.

Event wiring: no `ProfileUpdated`/`ProfileSectionsChanged` event existed, so added
`PROFILE_EVENTS.FACTS_CHANGED` (`profile.facts_changed`) in the new
`packages/domain/src/profile/events.ts`, emitted from `addSection`/`updateSection`/`removeSection`
(alongside their existing finer-grained events — additive, nothing removed). Judgment call:
deliberately NOT emitted from `CareerProfile.create` — a brand-new profile has zero sections, so
embedding it would just embed empty content; the use case itself also treats empty-content
profiles as a safe no-op (documented in both places) in case a profile is later emptied back out.
Worker handler `apps/worker/src/handlers/profile-facts-changed.handler.ts` registered in
`apps/worker/src/main.ts` alongside the existing job-posted worker, same guarded-LLM/outbox path.

Known limitation, documented not silently omitted: `ProfileRepository` has no
`withCareerProfileLock` equivalent to task 017's `withJobPostingLock`, so genuinely-simultaneous
(not just sequential) redelivery for the SAME profile isn't closed the way task 017 closed it for
job postings — out of this task's file list; flagged as a follow-up if profile-embed volume ever
makes it matter.

Evidence:
- Unit (`packages/application/test/unit/embed-career-profile.test.ts`, fake LLM + fake repo):
  7/7 passing — embed-and-ready, redelivery-is-free (LLM called once), model-change re-embeds,
  full facts-changed-then-stale-then-re-embed cycle through the use case twice, budget-block
  marks failed not crashed, not_found on deleted profile, empty-profile safe no-op.
- Domain (`packages/domain/src/profile/career-profile.test.ts`): all 21 pre-existing tests still
  pass unchanged after adding the second recorded event to addSection/updateSection/removeSection.
- Integration (`apps/worker/test/integration/profile-embed.test.ts`, REAL Postgres 16 + pgvector
  and REAL Redis/BullMQ via `docker compose up -d postgres redis` + `pnpm db:migrate`, not fakes):
  2/2 passing — full outbox → relay → BullMQ → worker → `embeddingStatus: 'ready'` path, and a
  second end-to-end cycle proving `isEmbeddingStale` correctly drives a real second LLM call
  after a section changes. Lives next to `end-to-end.test.ts` in `apps/worker/test/integration/`
  rather than under `packages/infrastructure/test/integration/` (the task file's suggested
  location) because it needs the worker's real BullMQ handler, which lives in `apps/worker` —
  same reasoning that already placed `end-to-end.test.ts` there.
- Full unit suite (377/377) and full integration suite (94/94, the pre-existing 92 plus these 2)
  pass together — no regression to existing job-posting/profile/document integration coverage.
- `tsc --noEmit` clean across domain/application/infrastructure/worker. `eslint` clean. Boundary
  lint passes.
