# 014 — CI pipeline + chaos test

**Status:** TODO | **Milestone:** M2

## Objective
Make the architecture's rules enforceable by a machine. A rule that doesn't fail a build is a suggestion.

## Files affected
- `.github/workflows/ci.yml`, `.github/workflows/nightly.yml`
- `e2e/chaos/worker-kill.spec.ts`

## Dependencies
001–013

## Acceptance criteria
- [ ] Pipeline: lint → typecheck → unit → integration (Testcontainers) → build → e2e (compose) → security (CodeQL, osv-scanner, gitleaks, trivy)
- [ ] Coverage gate **≥90%** on `domain` + `application`; build fails below
- [ ] **Boundary-violation test**: a fixture branch importing `infrastructure` from `domain` fails CI
- [ ] **Chaos test**: `SIGKILL` the worker mid-relay → zero lost events, zero duplicate rows
- [ ] Nightly: real-Ollama LLM contract test (kept out of the PR path — slow, and not everyone has a model)
- [ ] Branch protection on `main`: all green + 1 review

## Test plan
The pipeline is the test. Verify by deliberately breaking each gate once and confirming CI goes red.
