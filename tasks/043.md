# 043 — Budget hard-stop proof (chaos/integration test)

**Status:** TODO | **Milestone:** M5

## Objective
Deliver the M5 roadmap acceptance criterion "budget hard-stop demonstrated" with a real, evidence-backed test — not a claim. M5 introduces the system's first `complete()` call sites (038 matching, 039 tailoring, 040 verification) at real volume (an ANN-prefiltered batch of candidates per profile, each needing a scoring call); this task proves `GuardedLlmPort` + the real `CostEstimator` (033) + the Postgres advisory budget lock (tasks 015/016) actually stop spend at the configured limit under concurrent load from these new call sites, closing the same lost-update race class already fixed once for connector health counters (task 032) and proven once already for the budget primitive itself (task 015) — this task is the M5-scope regression proof, not new locking logic.

## Files affected
- `packages/infrastructure/test/integration/budget-hard-stop-m5.test.ts` (new) — real Postgres, real `PostgresBudgetStore`, a low test budget (e.g. $0.01), concurrent fan-out of matching (038) + tailoring (039) requests via `Promise.all`, assert: (a) total recorded spend never exceeds the budget, (b) at least one request is rejected once the budget is exhausted, (c) no `ai_invocations` row exists for a rejected request (the guard must reject before dispatch, not after).
- `apps/api/test/integration/` or a worker-level chaos test — end-to-end version hitting real routes/handlers if 038/039's API surface makes that practical (prefer this over a pure unit-level fake if the plumbing is already in place from 038/039).
- No production code changes expected — this task's job is to prove existing machinery (`GuardedLlmPort`, `PostgresBudgetStore.withUserBudgetLock`, `TieredCostEstimator` from 033) holds under M5's new call volume. If it finds a real bug (plausible — this is genuinely new concurrent load through the guard), fix it and document what broke and why in the Status note, same as tasks 015/016/032 did for their respective races.

## Dependencies
- 033 (real CostEstimator), 038 (matching), 039 (tailoring) — needs real call sites to fan concurrent load through.

## Acceptance criteria
- Test demonstrates a hard stop: spend never exceeds the configured budget across concurrent requests, with an exact count assertion (not a loose "probably fine" check — same rigor as task 015's original proof).
- If a race is found, it's fixed and the fix is verified by the same test going from failing to passing (paste both outcomes as evidence, same as the task 032 chaos-test precedent).

## Test plan
- The task's own new integration test IS the test plan — run it against real Postgres (Docker), multiple times if the race is timing-sensitive, and record actual pass output as evidence before marking DONE.

## Status
TODO
