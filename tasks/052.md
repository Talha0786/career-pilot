# 052 — Screencast + batch review queue UI

**Status:** TODO | **Milestone:** M6

## Objective
Web UI for `awaiting_review`: live CDP screencast of the browser-runner's context plus a field-value diff, and the batch review queue ADR-003 requires ("the UI supports a review queue where the user inspects N pre-filled applications and approves them in a batch... each approval still mints its own single-use token and each submission is still individually consented to").

## Files affected
- `apps/api/src/ws/hub.ts` — extend `HubEvent` (currently `JobEmbeddedEvent | DocumentTailoredEvent`) with an `ApplyTaskScreencastEvent`/`ApplyTaskUpdatedEvent` variant; reuse the existing `ConnectionHub`/`requireAuth` cookie-session pattern (per the inventory, no new transport library needed). Note the existing hub scopes by `userId` only, not by task — since ApplyTasks are user-owned this is acceptable, but confirm no cross-task leakage in the payload.
- `apps/browser-runner/src/screencast.ts` (new) — CDP screencast frames piped up to the API (via Redis pub/sub or a direct internal call — `browser-runner` has no existing pub/sub path to `apps/api`; pick the simplest that fits the existing infra, e.g. publish to a Redis channel `apps/api`'s WS layer subscribes to, document the choice).
- `apps/web/src/app/apply/review/page.tsx` (new) — the batch review queue: lists all `awaiting_review` ApplyTasks for the user, per-task field diff (mapped value vs. taxonomy field vs. what will be submitted), approve/reject actions.
- `apps/web/src/components/{ApplyReviewCard,FieldDiffView}.tsx` (new).
- `apps/api/src/routes/apply.ts` (new) — `GET /apply-tasks?stage=awaiting_review` (list for queue), `POST /apply-tasks/:id/approve` (calls 046's `ApprovalTokenPort.mint`, transitions `awaiting_review → approved`), `POST /apply-tasks/:id/reject` (transitions to `aborted`).
- Sensitive fields (EEO/demographic) must render in the diff **unfilled and clearly marked** per §4's "never auto-filled; surfaced to the user untouched" — this is a UI-level restatement of the same invariant 048/049/050 enforce upstream; verify it isn't silently dropped from the diff view.

## Dependencies
- 051 (produces the `awaiting_review` state and diff data this UI reads), 046 (approval token minted on approve).

## Acceptance criteria
- A user can review multiple `awaiting_review` tasks in one sitting and approve each individually — each approval mints its own token (verify via 046's port, not a shared/batch token).
- Screencast renders live frames from a real browser-runner context during `mapping`/`filling` (not just a static screenshot) — smoke-testable in a real browser.
- Sensitive fields are visibly unfilled/flagged in the diff, never silently pre-populated.
- 30-min `awaiting_review` timeout (design doc §3) surfaces in the UI (task disappears from the active queue / shows as aborted) once it fires.

## Test plan
- Component/unit: diff rendering with fixture data including a sensitive field, confirming it renders unfilled+flagged.
- Integration: API route round-trip (list, approve mints token, reject aborts).
- Manual/browser verification (per this session's standing rule for UI changes): actually open the review page in a browser against the running compose stack and confirm the golden path.

## Status
DONE — the field-level review diff (ADR-003's actual requirement: "a human must review a
field-level diff... before every submission") is now live-wired end to end, not just a
presentational shell. Live CDP screencast remains a documented, deliberate gap (see below) — a
real UX nicety, not the safety property this milestone is graded on; the 30-min timeout sweep
also remains unbuilt, documented honestly.

**The field-diff data flow (the follow-up this task needed, now real):**
1. `apps/browser-runner/src/fill-runner.ts` — each per-field `apply_task_steps` row (already
   append-only per task 044/051) now also carries `confidence`/`source`/`neverAutoFill` (never the
   raw filled VALUE — §6's redaction rule stays exactly as designed for that audit row).
   `packages/application/src/ports/field-detection.port.ts`'s `DetectedField` gained an explicit
   `source: 'known_ats' | 'heuristic' | 'llm'` tag, threaded through from `field-detection.adapter.ts`
   (048/049 matches) and `map-fields.ts` (050/LLM matches) rather than inferred later from a
   confidence-score heuristic, which would have been ambiguous.
2. `packages/application/src/ports/repositories.ts` — `ApplyTaskRepository.listSteps()` (new), the
   review-diff read source; implemented in `DrizzleApplyTaskRepository`, `FakeApplyTaskRepository`.
3. `apps/browser-runner/src/task-api.ts` — `GET /internal/tasks/:id/fields` (new): reconstructs the
   field list from `apply_task_steps` (last-write-wins per selector), and for every NON-sensitive
   field reads the value back from the LIVE PAGE DOM (`page.inputValue`) — this is the only honest
   source for "what will actually be submitted" (the audit log is deliberately redacted). Sensitive
   fields ALWAYS report `mappedValue: null`, unconditionally, with no code path that could read
   one from the DOM even by a future bug upstream.
4. `apps/api/src/lib/browser-runner-client.ts` (`HttpBrowserRunnerFieldsClient` /
   `BrowserRunnerFieldsPort`) + `apps/api/src/routes/apply.ts`'s new `GET /apply-tasks/:id/fields`
   — ownership-checked (404 for another user's task), stage-gated (409 outside
   `awaiting_review`/`approved`), proxies to browser-runner.
5. `apps/web/src/lib/api/apply.ts` + `review/page.tsx` + `ApplyReviewCard`/`FieldDiffView` — each
   card in the queue has a "Review field diff" toggle that fetches and renders the REAL diff for
   that task, including a sensitive field rendered unfilled-and-marked per §4.

**Still honestly incomplete (documented, not silently dropped):**
1. **Live CDP screencast** — not built; judged lower priority than the diff-data correctness work
   within this milestone's remaining time. No visual monitoring is a lesser gap than no diff data
   at all (the coordinator's own framing, and the right call: ADR-003 requires the diff, not video).
2. **30-min `awaiting_review` timeout** — not implemented as an active sweep;
   `ApplyTaskBrowserContextManager.activeTaskIds()` remains the hook point for one.
3. Manual/browser verification of the rendered page in an actual browser was still not performed
   in this session (no `apps/web` dev server was run) — the component code typechecks and every
   API layer it calls is proven for real via integration tests; the literal pixels are unverified.

**Evidence:**
- Integration (`apps/api/test/integration/apply.test.ts`, real Postgres + real Redis): 6/6 (was
  4/4; +2 for this follow-up) — the 4 original tests plus: `GET /apply-tasks/:id/fields` returns
  409 before `awaiting_review`, 200 with the real (scripted) browser-runner data once there
  (including the sensitive-field-always-null assertion), and 404 for a cross-owner request; auth
  check extended to cover the new route too.
- Integration (`apps/browser-runner/test/integration/task-api.test.ts`): 8/8 (was 4/4; +4) — 404
  for an unknown task; reconstructs the field list from `apply_task_steps` correctly, ignoring
  non-field (stage-transition) steps, sensitive field always null; last-write-wins when the same
  selector appears in two steps (e.g. an error retry); 501 when the route is configured without
  its `applyTasks` dependency.
- Playwright (`apps/browser-runner/test/fill-runner.spec.ts`): 3/3, re-verified — confirms the
  `source`/`confidence` metadata threading didn't break the existing real-DOM fill behavior.
- `tsc --noEmit` clean across all 14 packages, `eslint` clean (0 errors, 6 pre-existing unrelated
  warnings), boundary lint passes.
