# 012 — Web: Next.js auth + live board

**Status:** TODO | **Milestone:** M2

## Objective
Minimal UI proving the async spine end-to-end: paste a job, watch it go `pending → ready` with no page refresh.

## Files affected
- `apps/web/src/{app/(auth)/{login,register},app/board,features/{auth,jobs,board},lib/{api-client,ws-client}}`

## Dependencies
011

## Acceptance criteria
- [ ] Register / login / logout against the API session cookie
- [ ] Paste-job form → card appears immediately as `pending`
- [ ] WebSocket flips the card to `ready` **without a refresh** (this is the whole point of M2)
- [ ] Embedding failure renders a clear failed state — not a spinner forever
- [ ] Drag a card between stages → persists across reload
- [ ] Types imported from `packages/contracts` — no hand-rolled duplicates

## Test plan
E2E (Playwright, full compose stack): register → login → paste → `pending` → `ready` via WS → drag stage → reload persists.

## Notes
Deliberately plain UI. Design work is M11 (Dashboard). Resist polish here.
