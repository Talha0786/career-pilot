# 006 — Drizzle schema + migration 0001 (Postgres only)

**Status:** TODO | **Milestone:** M2

## Objective
Executable schema for the M2 subset. Postgres is the sole dialect (ADR-002 amendment) — no SQLite.

## Files affected
- `packages/infrastructure/src/db/schema/{users,job-postings,applications,stage-transitions,outbox,ai-invocations,audit-log}.ts`
- `packages/infrastructure/src/db/migrations/0001_init.sql`
- `drizzle.config.ts`

## Dependencies
001

## Acceptance criteria
- [ ] Migration creates `CREATE EXTENSION IF NOT EXISTS vector` and `citext`
- [ ] Only the tables the slice needs (design §4). No speculative schema.
- [ ] Indexes: unique `users.email`; `job_postings (user_id, ingested_at desc)`; `job_postings.url_hash`; **partial** index on `outbox (created_at) WHERE published_at IS NULL`; `ai_invocations (user_id, created_at)`
- [ ] **No HNSW index yet** — deferred to M5; index choice needs real cardinality
- [ ] Migration runs clean from empty DB; `drizzle-kit check` passes

## Test plan
Integration (Testcontainers, real Postgres 16 + pgvector): migrate from scratch, assert tables/indexes/extensions exist.
