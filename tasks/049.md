# 049 — Heuristic field detection

**Status:** TODO | **Milestone:** M6

## Objective
Stage 2 of the field-mapping pipeline (`docs/05-playwright-design.md` §4): when no known-ATS adapter matches (048's `detect()` returns false for all four), fall back to label/`name`/`autocomplete`/aria-attribute heuristics mapping arbitrary forms into the same canonical taxonomy.

## Files affected
- `apps/browser-runner/src/heuristic-mapper.ts` (new) — walks a page's accessible form tree, scores candidate fields against the taxonomy (048's `taxonomy.ts`) using label text, `name`/`id` attribute patterns, `autocomplete` values, `aria-label`/`aria-describedby`, and input `type` (email/tel/file). Returns per-field confidence scores, not just booleans — low-confidence fields feed into task 051's "leave blank and flag" behavior.
- `apps/browser-runner/test/fixtures/heuristic-corpus/*.html` (new) — corpus of 100+ real (sanitized) form snapshots per `docs/05-playwright-design.md` §7's testing table ("Heuristic mapper | Unit tests on a corpus of 100+ real (sanitized) form snapshots"). If 100 genuinely distinct real-world forms aren't practically obtainable, document the actual corpus size and composition honestly in the Status note rather than padding with near-duplicates — this is a case where an honest smaller number beats a fake-looking 100.
- `apps/browser-runner/test/heuristic-mapper.spec.ts` (new).

## Dependencies
- 048 (shared taxonomy).

## Acceptance criteria
- Sensitive fields (EEO/demographic/veteran/disability) are never assigned a non-zero auto-fill confidence, even when a form's labels superficially resemble them — same never-auto-fill invariant as 048, enforced independently here since this stage runs on forms the known-ATS maps never saw.
- Confidence scoring is monotonic-sane: an exact `autocomplete="email"` match scores higher than a fuzzy label-text guess (document the scoring rubric, even informally, so 051's "low confidence → blank + flag" threshold is a real decision, not arbitrary).

## Test plan
- Unit tests against the corpus fixtures — precision/recall on the P0 fields (name, email, phone, resume-upload) reported as a real number, not just "looks right."

## Status
DONE.

**Honest corpus-size disclosure (per this task's own explicit permission to do so rather than
pad):** the task file's own testing plan names "100+ real (sanitized) form snapshots" as the goal,
and explicitly says: "if 100 genuinely distinct real-world forms aren't practically obtainable,
document the actual corpus size and composition honestly... this is a case where an honest smaller
number beats a fake-looking 100." This sandbox has no network access to collect real forms. Built a
hand-crafted SYNTHETIC corpus instead: **14 distinct form snapshots**, consolidated into one file
(`test/fixtures/heuristic-corpus/corpus.html`, multiple `<form>` sections — a pragmatic deviation
from the task file's literal "*.html" plural-files phrasing, noted in that file's own header
comment) covering: Workday/iCIMS/Taleo/BambooHR/SmartRecruiters-style naming conventions,
autocomplete-attribute-only fields, aria-label-only fields, placeholder-only fields, fuzzy/contains
name-id matches, a non-English-labeled form (name/autocomplete signal still resolves), decoy fields
that must NOT map to anything, EEO fields disguised under several real-world euphemisms
("self-identification", "protected veteran status"), and decoys that superficially resemble EEO
topics but aren't ("race pace" for a running club, "pronouns for team intros"). ~85 annotated
fields total across the 14 forms.

**Scoring rubric** (documented in `heuristic-mapper.ts`'s own doc comment, highest-signal-wins, not
additive): exact WHATWG `autocomplete` token (1.00) > exact name/id match (0.85) > fuzzy name/id
(0.65) > label text (0.55) > aria-label (0.45) > placeholder (0.35), with input `type` as a
same-signal BOOST only (+0.10, never sufficient alone — `type="file"` alone doesn't manufacture a
`resumeUpload` match without a name/label/aria signal too). `neverAutoFill` taxonomy keys always
score 0 regardless of match strength — proven directly by a unit test asserting an exact-id match
against `eeoGender` still returns 0.

**Real bug found and fixed during task 054's e2e verification (cross-referenced here since it
lives in this file):** radio-group fields were originally extracted per-option
(`#work_authorization_yes`, `#work_authorization_no`), which never string-matched a known-ATS map's
group-level selector (`input[name="work_authorization"]`), causing task 051's confidence-matching
to wrongly treat known-ATS-covered radio groups as unresolved and fall through to the LLM stage.
Fixed by excluding `type="radio"` from `extractFormFields` entirely — radio groups (all P1, not
P0) are handled through known-ATS group selectors only for now; generic per-option
heuristic/LLM radio mapping is a legitimate follow-up. Verified this doesn't change the P0
precision/recall numbers (workAuthorization/sponsorshipRequired are P1, outside the measured set)
by re-running the full Playwright suite before/after — identical TP/FP/FN.

**Files:** `apps/browser-runner/src/heuristic-mapper.ts` (`scoreFieldConfidence`,
`mapFormFieldsHeuristically`, `extractFormFields`), `test/fixtures/heuristic-corpus/corpus.html`,
`test/heuristic-mapper.spec.ts` (Playwright, corpus eval), `test/unit/heuristic-mapper.test.ts`
(vitest, pure rubric logic — required extending `vitest.config.ts`'s unit-test include glob to
also cover `apps/*/src|test/unit` since no `apps/*` package had fast pure-logic unit tests before
this milestone, documented in that config file's own comment).

**Evidence:**
- Unit (`apps/browser-runner/test/unit/heuristic-mapper.test.ts`): 7/7 — exact autocomplete scores
  highest; monotonic ordering (autocomplete > exact name/id > fuzzy label > nothing); sensitive
  field always scores 0 even against an exact-grade signal; type is a boost, never sufficient
  alone; sensitive-field assignment in the full mapper always confidence 0; below-floor fields left
  unmapped, not guessed; higher-confidence field wins a contested taxonomy key.
- Playwright (`apps/browser-runner/test/heuristic-mapper.spec.ts`, real corpus, real DOM extraction):
  3/3 — never assigns non-zero confidence to a sensitive field across all 14 forms; decoy fields
  never steal a real field's taxonomy key; **P0 precision/recall printed as real numbers: TP=27
  FP=0 FN=4 → precision=100.0%, recall=87.1%** (gate: ≥90% precision / ≥80% recall, both met).
- Full unit suite 555/555 (includes this task's 7), `tsc --noEmit` clean, `eslint` clean, boundary
  lint passes.
