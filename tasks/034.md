# 034 — Prompt file convention + PromptStore loader

**Status:** TODO | **Milestone:** M5

## Objective
Establish the `prompts/{task}/{version}.md` convention referenced by `docs/06-agent-design.md` §2 ("Prompts are versioned files with frontmatter (model tier, temperature, output schema ref). Prompt changes are code-reviewed like code.") and give the application layer a port to load them, since no such convention or loader exists anywhere in the repo yet.

## Files affected
- `prompts/match-score/v1.md` (new) — frontmatter: `model_tier`, `temperature`, `output_schema` (ref name, e.g. `ScoreComponentsSchema`); body is the actual system/user prompt template with `{{placeholders}}`.
- `prompts/tailor-resume/v1.md`, `prompts/tailor-cover-letter/v1.md` (new, stub content — task 039 fills in the real template logic, this task only needs the loader + one working example to prove the mechanism).
- `prompts/verify-claims/v1.md` (new, stub — task 040 fills in).
- `packages/application/src/ports/prompt-store.port.ts` (new) — `interface PromptStore { load(task: string, version?: string): Promise<Result<PromptTemplate, PromptError>> }`, `PromptTemplate { frontmatter: {modelTier, temperature, outputSchema}, render(vars: Record<string,string>): string }`. Defaults to latest version when `version` omitted (highest semver-like filename in the task's directory).
- `packages/infrastructure/src/prompts/file-prompt-store.ts` (new) — `FilePromptStore implements PromptStore`, reads from the `prompts/` dir (path injected, not hardcoded — so tests can point at a fixture dir), parses frontmatter (reuse an existing lightweight frontmatter parser if one exists in the repo's deps already — check `pnpm-lock.yaml` before adding a new dependency; a hand-rolled `---\n...\n---` splitter is acceptable, do not add a heavy YAML frontmatter library for this).
- `packages/infrastructure/test/unit/file-prompt-store.test.ts` (new).

## Dependencies
None (foundational; can run in parallel with 033 and 035).

## Acceptance criteria
- `FilePromptStore.load('match-score')` returns the parsed frontmatter + a `render()` that substitutes `{{placeholders}}` and throws/`Result`-errors on an unfilled placeholder (fail loud, not silently pass `{{foo}}` through to the LLM).
- Missing task dir / missing version → typed `PromptError`, not an unhandled throw.
- Boundary lint passes: `PromptStore` port in application (zero infra imports), `FilePromptStore` impl in infrastructure.

## Test plan
- Unit: load + render happy path, missing-placeholder failure, missing-file failure, version-resolution (latest-wins when omitted).

## Status
TODO
